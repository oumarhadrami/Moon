<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".home_screens.cart.PlaceOrderFragment">

        <FrameLayout
            android:id="@+id/place_order_framelayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <include
                android:id="@+id/order_placing_screen"
                android:visibility="gone"
                layout="@layout/placing_order_layout"/>

            <include
                android:id="@+id/order_placed_screen"
                android:visibility="gone"
                layout="@layout/order_placed_layout"/>
        </FrameLayout>
    </FrameLayout>
</layout>